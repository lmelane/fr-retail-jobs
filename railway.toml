# Root config. Each service overrides dockerfilePath and startCommand via the
# Railway API (see `railway variables` / service settings), because a monorepo
# builds several images from one repository.
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/aggregator/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
