# CLAUDE.md — Catwalks Job Aggregator

> Ce fichier grave les **décisions métier** et l'**état vérifié** de la plateforme.
> Règle : le **code + la base + les logs de prod** sont la source de vérité, jamais un résumé.
> Toute décision métier structurante se prend avec le propriétaire (Loïc), jamais seul.

## Le produit, en une phrase

Un **jobboard/agrégateur gratuit** qui réunit **toutes les offres publiques Mode · Luxe · Beauté · Horlogerie · Retail disponibles en France**, sans doublon, avec le texte complet et le lien de candidature direct. Deux flux de rang égal : (A) les ATS/pages carrière des employeurs, (B) les jobboards et cabinets. Déduplication à l'écriture : **1 offre canonique + N sources**.

## Parties prenantes

- **Propriétaire / décideur produit** : Loïc (loic.melane.pro@gmail.com). Décide de toute intention métier.
- **Candidats** : cherchent, filtrent, lisent une offre complète, postulent chez l'employeur. Aucune inscription.
- **Employeurs / Maisons** : leurs offres sont agrégées depuis des sources publiques et autorisées (robots.txt lu à la source).

## Infra (vérifié le 2026-09-02)

Projet Railway **« data feed of jobs »**, env `production` :

| Service | Rôle réel |
|---|---|
| `Postgres` | base de prod (postgres-ssl:18), volume monté, réseau interne uniquement |
| `catwalks-web` | site Next.js — `catwalks-web-production.up.railway.app` |
| `catwalks-aggregator` | cron `ingest` |
| `catwalks-refresh` | cron `refresh` (lifecycle) |
| `catwalks-reconcile` | **anomalie : exécute `ingest`, pas `reconcile`** (à corriger) |

Monorepo npm workspaces : `apps/aggregator` (pipeline), `apps/web` (site), `packages/db` (Prisma).
La base de prod n'est joignable que via le réseau interne Railway → diagnostics via `railway ssh --service catwalks-aggregator` (lecture seule pour l'audit).

## Décisions métier gravées

### D1 — DB indisponible → page d'erreur propre, jamais d'offres fictives
Si la base est injoignable, le site rend une **page d'erreur propre (503)**, pas d'offres inventées. Le fallback `DEMO_JOBS` (6 offres fictives) est **supprimé**. _Un jobboard ne ment jamais sur ses offres._ (Décidé 2026-09-02.)

### D2 — Les filtres Groupe et Source sont activés dans l'UI
La barre de filtres expose **Secteur · Contrat · Ville · Maison · Groupe · Source**. Le groupe et la source, aujourd'hui morts-nés dans le code, deviennent des filtres réels (comptes exacts), conformément à la promesse produit (« Kering ouvre ses 14 Maisons »). (Décidé 2026-09-02.)

### D3 — `Job.source` stocke le vrai ATS
`Job.source` (AtsType) doit refléter la **vraie source** (WORKDAY, GREENHOUSE, LVMH_ALGOLIA…), pas la valeur figée `GENERIC_JSONLD`. Corrige la contrainte unique `(companyId, source, externalId)` et le chemin de récupération après course. (Décidé 2026-09-02.)

### D4 — Fresh start, versioning conservé, legacy d'affichage supprimé
On **repart d'une base propre** une fois. On **garde** `pipelineVersion` comme garde-fou des futurs correctifs de schéma. On **supprime** `readableDescription` (nettoyage des vieilles lignes) : les nouvelles lignes sont déjà propres. (Décidé 2026-09-02.)

### D5 — Suppression du legacy à 100 %
Tout code mort / de compatibilité inter-générations / rustine temporaire est **supprimé**, pas conservé « au cas où ». Une décision métier cachée dans du legacy remonte en decision card avant suppression.

### D6 — Ingest découpé par source, purge conditionnelle au succès
Chaque source (ou petit lot) devient un **run court et indépendant**. La purge par génération n'efface une source que **si son fetch a réussi** — jamais en tête de run global. Un flux qui casse n'empêche plus les autres ni ne vide la base. (Décidé 2026-09-02.)

### D7 — Chantier en local d'abord, prod à la fin
Toutes les corrections et la reconstruction de base se font **en local** (Postgres Docker isolé). On ne déploie/relance les crons de prod qu'une fois la plateforme **validée de bout en bout**. Aucune écriture en prod pendant le chantier. (Décidé 2026-09-02.)

### D19 — MONDE par défaut partout (révise D12), et le monde doit être ATTEIGNABLE
Le site affiche **toutes les offres (monde) par défaut** ; le filtre **Pays** restreint (FR, IT…). Révise D12 (« FR par défaut ») : le candidat voyait « France » figé, l'autocomplete ville/titre était câblé `isFrance:true`, et taper « Milan » ne renvoyait rien → les ~26k offres monde étaient **invisibles depuis la barre**. Corrigé : `parseFilters`/`parseCompanyFilters` défaut `country=undefined` ; `suggestCities`/`suggestTitles` sans filtre FR ; champ Lieu vide (placeholder « Ville, région ou pays ») ; sitemap élargi au monde. `isFrance` reste stocké et sert le filtre France. Vérifié : New York → 22, London → 9 (avant : injoignable). (Décidé et validé par Loïc via AskUserQuestion, 2026-09-02.)

### D20 — Page entreprise : barre de filtre légère (ville + contrat)
Une Maison à plusieurs centaines d'offres (Cartier : 1 173) était un mur infini sans filtre. Ajout d'une **barre légère scopée à la Maison** : recherche **ville** + pills **contrat** (facettes réelles), refetch serveur `/api/jobs?maison=…&ville=…&contrat=…` (chaque match vu, pas seulement le slice chargé). Pas la barre complète de `/emplois` (page déjà scopée à une société). Compteur d'offres et facettes de la fiche passés en **monde** pour coller à la liste. (Décidé et validé par Loïc via AskUserQuestion, 2026-09-02.)

### D21 — Attribution UTM : déjà bout-en-bout côté Catwalks, rien à construire
Le tracking « candidat inscrit via Fashion Atlas » est **déjà complet côté Catwalks** (vérifié à la source) : `capturerPremierContact()` (`premier-contact.ts`, monté globalement via `CapturePremierContact.tsx`) lit `utm_source/medium/campaign` à la **première** visite, stocke en `localStorage` sans jamais écraser (premier contact ≠ dernier), et le **backend + PostHog** l'attribuent à l'inscription. Les 3 CTA Atlas (footer, job-detail, landing) portent déjà `?utm_source=fashion-atlas&utm_medium=aggregator` (+ `utm_campaign` par surface). **Aucun dev côté Atlas** au-delà de porter les UTM. _Le code de l'état technique est la source : ne pas re-construire un tracking qui existe._ (Vérifié 2026-09-02.)

### D22 — Le catalogue doit VIVRE : datePosted stable, auto-index Google, expiration → 410/redirect
Constat gravé (vérifié en base 2026-09-02) : **0 offre n'a un `firstSeenAt` > 24h** → la base est wipe+rebuild à chaque run (churn), ce qui réinitialise `datePosted` (lu par Google Jobs) et déstabilise les IDs/URLs. Le correctif code existe déjà (`upsert.ts` estampille `pipelineVersion` à chaque touche, pas qu'au create) — **stabilité vérifiée en prod** (SourceRun : `previousJobs ≈ jobs`, ex. hermes 581→581, sandro 533→533 : les sources conservent leurs offres run-to-run, plus de churn). Décisions Loïc, **implémentées 2026-09-03** : (a) **soumission automatique à l'Indexing API Google** des nouvelles/périmées offres à chaque passe — `googleIndexing.ts`, JWT service-account signé avec `crypto` natif, câblé dans `ingest-all`, **no-op tant que `GOOGLE_INDEXING_CREDENTIALS` + le vrai domaine ne sont pas configurés** (code prêt, dormant) ; (b) **offre périmée → 410 Gone** — `middleware.ts` + `/api/offre-status/[id]` (410+noindex pour fermée, 404 pour inexistante, 200 pour active), vérifié en prod. **Action externe restante** : brancher le vrai domaine + provisionner le service account Google. (Décidé par Loïc 2026-09-03 ; (a) code prêt / (b) live.)
> **D22 révisé (refonte UI, validé Loïc 2026-09-03)** : le 410 d'une offre fermée ne sert plus une **page-stub codée en dur** ; le middleware fait désormais `rewrite` vers la vraie page `/offre/[id]` **avec `status:410` + `x-robots-tag:noindex`** → Google déréférence pareil, MAIS le candidat voit l'offre réelle avec un **bandeau « Expirée »** + un pont « Voir les offres similaires » (DA §4.13). `getJobStatus` renvoie l'offre aussi en `closed` (aucune requête en plus) ; le JSON-LD JobPosting est retiré sur une offre fermée. Vérifié en local : `/offre/<fermée>` → HTTP 410 + page rendue.

### D23 — La chaîne d'hygiène A→Z : le validateur est la SOURCE, pas un HTTP HEAD
Un agrégateur ne montre que des offres réelles avec des liens vivants. La chaîne (validée avec Loïc) : (1) Maison entre → détection ATS ; (2) ingest depuis l'ATS (Flux A) avec la **bonne URL** (fix adapters Magnet/Workday/Eightfold + propagation `Job.url` aux lignes existantes) ; (3) **VALIDATION = présence dans le feed de la source** — gratuit (on re-crawle déjà), exhaustif, vrai. Un HTTP HEAD par offre est **rejeté comme primaire** : à 14k+ maisons c'est des millions de requêtes souvent bot-bloquées (FashionJobs/WTTJ renvoient 403 en étant vivants → le HEAD ment ; le feed ne ment pas). Le HEAD reste un **filet de sécurité ponctuel**. (4) Lien mort → **re-crawl source d'abord** (self-heal via `refresh.ts` : une offre survit tant qu'une source la re-liste ; fermée seulement quand toutes ses sources sont stale ; ré-ouverte si la source revient). (5) Offre fermée → **410 Gone** (implémenté : `middleware.ts` + `/api/offre-status/[id]` → 410 pour une offre `isActive:false`, 404 pour un id inexistant, `noindex` sur le 410). (Décidé et validé par Loïc, 2026-09-03.)

### D24 — Alerte email Brevo sur source dégradée/en panne (observabilité)
À 14k+ sources, une panne silencieuse est l'ennemi (FashionJobs a tourné 0 fois pendant des jours sans signal). Un **digest email par run** liste chaque source DEGRADED/BROKEN → `loic.melane@catwalks.io`. Déclencheur = **source qui meurt** (0 offre, chute anormale), jamais un email par lien mort (une offre qui ferme = normal et constant). Transport = **Brevo** (le fournisseur que Catwalks utilise déjà — `api.brevo.com/v3/smtp/email`, header `api-key` ; ne pas réinventer). No-op propre si `BREVO_API_KEY` absent. **Config prod requise** : ajouter `BREVO_API_KEY`, `BREVO_SENDER_EMAIL`, `BREVO_SENDER_NAME` aux variables Railway du service `catwalks-aggregator` (elles existent côté Catwalks, à recopier — action Loïc). (Décidé et validé par Loïc, 2026-09-03.)

### D25 — Hygiène réseau GLOBALE : politesse par hôte + dégradation gracieuse (pas de patch par adaptateur)
Le même échec revenait partout : plusieurs marques partagent un hôte (`careers.elcompanies.com` pour 6 marques Estée Lauder, `richemont.wd3…` pour toutes les Maisons Richemont, un tenant Beaumanoir, le rate-limiter de Courir), on le martèle en parallèle, il nous throttle (403/405/429), et les offres d'un groupe entier tombent à un blip. **Corriger la cause une fois pour TOUTE requête sortante**, pas par rustine d'adaptateur (règle Loïc, 2026-09-03). Solution : `hostGate.ts` — une **porte par hôte** (concurrence limitée + délai minimum entre requêtes au même hôte + **backoff adaptatif** : un hôte qui throttle voit son délai grandir puis décroître au succès), appliquée dans `fetchWithRetry` ET le transport navigateur (`fetchRenderedHtml`) → **tous les adaptateurs + la découverte 14k en bénéficient automatiquement**. Des hôtes différents ne s'attendent jamais (débit global inchangé). Corollaire : une étape optionnelle qui échoue (ex. `openSession` Eightfold, qui n'est PAS requise — la search API répond 200 sans cookie) **dégrade gracieusement** au lieu de couler la source. (Décidé par Loïc, 2026-09-03 ; implémenté + testé.)

> **D26 EXÉCUTÉ EN PROD (2026-09-03, ~18h-19h UTC)** — l'ordre a été suivi et chaque étape mesurée :
> (1) dump prod 63 MB via proxy TCP temporaire, **restauré en local** (26 818 Jobs / 26 817 actives / 528 Companies / 35 572 JobSource) ;
> (2) répétition `separate-fused` sur la copie : fusedBefore **3 709** → separated 8 636 → fusedAfter **0**. La répétition a attrapé un bug de déploiement réel : la baseline `--applied init` mentait (prod, née en `db push`, n'avait pas `Job.language`) → migration de rattrapage `20260903190000_language_pre_init_backfill` (IF NOT EXISTS) ajoutée AVANT le deploy ;
> (3) code déployé (PR #11 → main, 4 services SUCCESS) ; attente de la quiescence des écritures de l'ancien conteneur avant toute op ;
> (4) prod : migrations (resolve init + deploy) ✓, `import-sources` 83/83 ✓, `separate-fused` : fusedBefore **3 712** → separated **8 639** → reattached 1 → **fusedAfter 0** ; re-run de contrôle : fusedBefore **0** ;
> (5) `retire-source` × 18 (cartier-3 + les 17 clés D-28) : **5 857 JobSource détachées, 5 842 offres fantômes supprimées, 2 URLs canoniques réassignées** ; vérifié : **0 offre active sous un nom « +N »** ;
> État final avant le premier ingest gen-7 (cron 00:00) : **29 703 Jobs (29 702 actives), 83 sources ACTIVE**. Proxy TCP supprimé après les opérations. Backups Railway Daily+Weekly activés (plan Pro) + premier snapshot créé.
> Reste pour clore D26 définitivement : `fusedAfter = 0` re-mesuré à J+7 (mode métrique hebdo).

### D26 — Réparation D-01 (dédup intra-source) : ordre des opérations OBLIGATOIRE
Constat mesuré en prod (lecture seule via railway ssh, 2026-09-03) : **3 707 offres sur 26 786 actives (13,8 %) sont des fusions abusives** — plusieurs vrais postes d'une même source cachés derrière une seule offre affichée, car le garde « même source, ids distincts » ne se déclenchait jamais à l'écriture. Le correctif (branche `aggregator/validation`) + la commande `separate-fused` existent. **Ordre imposé** : (1) pg_dump du Postgres Railway, **restauré une fois en local — la sauvegarde n'existe que si la restauration a été prouvée** ; (2) `separate-fused` sur ce dump local (répétition + chiffre exact) ; (3) déploiement du nouveau code d'écriture ; (4) `separate-fused` en prod **avant le prochain ingest** — lancé avec l'ancien code encore en cron, le run suivant re-fusionne tout. Le Job conservé garde id/URL/firstSeenAt (stabilité D22) ; chaque id détaché repart avec le firstSeenAt de sa JobSource. Métrique de non-régression : `fusedAfter` = 0, pour toujours (la commande exit 1 sinon). (Décidé par Loïc, 2026-09-03.)

### D27 — Une source retirée du catalogue passe par `retire-source <clé>`
Supprimer une ligne de `sources.csv` n'a AUCUN cycle de vie : la purge générationnelle ne touche jamais ses lignes (la source ne tourne plus), le refresh les ferme après 48 h mais elles restent en base, inactives, sous leur étiquette (cas réel : ~1 300 offres « Cartier +3 » de la route Richemont interdite par robots). `retire-source` détache les JobSource de la clé retirée, supprime les offres que rien d'autre n'atteste (D1), et réassigne l'URL canonique à la meilleure source restante. À exécuter à CHAQUE retrait de ligne, et à chaque abandon d'un flux B. (Décidé par Loïc, 2026-09-03.)

### D32 — ANNULÉE le 2026-09-04 : le « trou noir SAP » n'existe pas
> **Réfutée par la sonde d'egress dans le VRAI run du cron** (protocole imposé par Loïc). Mesure du 2026-09-04 à 12:00:52 UTC, exécutée en tête de process — avant hostGate, avant l'ingest, avant la DB — depuis `catwalks-aggregator` (IP publique `152.55.184.233`, AS400940 Railway NL, région europe-west4-drams3a) :
> `dns A jobs.dolcegabbana.com` → **130.214.193.81** (l'IP accusée) · `curl -4 direct` → **http=200**, connect **11 ms**, TLS 46 ms, TTFB 326 ms · `fetch Node direct` → **http=200, 16 liens d'offres** · `fetch via hostGate` → **OK, 16 liens d'offres**. Deux mesures indépendantes à 2 s d'écart, identiques. **Le TCP vers 130.214.193.81 ne timeout pas : il se connecte en 11 ms et rend de vraies offres.**
> Et les sources elles-mêmes tournent, dans ce même run : `dolce-gabbana` **73 offres**, `aptar-beauty` **210**, `l-occitane-en-provence-2` **15** — exactement les volumes que je croyais perdus. Il n'y a jamais eu de panne.
> **Erreur de méthode à ne pas refaire** : le diagnostic d'origine a été fait dans un conteneur `railway ssh` réveillé à la demande, **dont l'egress n'est pas celui du conteneur du cron**. Un environnement voisin n'est pas la source. La règle qui en sort : une cause réseau ne se grave que sur une mesure prise **dans le processus réel qui échoue**, au moment où il échoue. Les points (a) « mesurer la joignabilité de l'IP » et (b) « tester le TCP avant d'ajouter un tenant RMK » restent de bons réflexes ; les points (c) et (d) — 7 sources BROKEN, décision de dépense d'un proxy sortant — sont **sans objet**. Le test Static Outbound IP est **abandonné**, faute d'objet.
> Texte d'origine conservé ci-dessous comme trace de l'erreur.

<details><summary>D32 (texte d'origine, FAUX)</summary>

Constat mesuré à la source (2026-09-04, depuis le conteneur Railway) : 7 sources SuccessFactors sont passées BROKEN le même run (~640 offres). **Ce n'était ni l'adaptateur, ni les configs, ni les Maisons** — l'adaptateur, lancé depuis un poste local sur les MÊMES configs, retourne exactement les volumes perdus (Aptar 209, D&G 73, Coty 125, L'Occitane 15). Diagnostic réseau : le DNS résout, mais le **TCP 443 expire** (`UND_ERR_CONNECT_TIMEOUT`) depuis Railway vers **`130.214.193.81`**, le frontal historique SAP RMK. Scan de 20 hôtes RMK du secteur : **14 pointent sur cette IP → tous TIMEOUT** ; les 6 servis par d'autres frontaux (Cloudflare `104.19.x`, Google `34.x`, Azure `20.x`) → **tous OK**. Conséquences gravées : (a) ne JAMAIS diagnostiquer une panne groupée par l'adaptateur avant d'avoir mesuré la joignabilité de l'IP de destination — la panne groupée par hôte partagé est le motif récurrent (cf. D25) ; (b) **avant d'ajouter un tenant RMK au catalogue, tester le TCP depuis la prod** — un tenant sur `130.214.193.81` entrerait BROKEN par construction ; (c) les 7 sources concernées restent BROKEN et VISIBLES (digest Brevo), leurs offres ne sont pas fermées à tort (le refresh exclut BROKEN) — on préfère 640 offres absentes à 640 offres périmées affichées (D1) ; (d) le déblocage est une décision de dépense de Loïc (proxy sortant / exécution hors Railway pour ces sources), non prise par délégation.

</details>

### D33 — Détection ATS : deux trous mesurés, et l'URL carrière est INVENTÉE dans 45 % des cas
Mesure du 2026-09-04 sur les **232 tenants encore « descriptifs »** du référentiel (sonde `probeDescriptiveTenants`, lecture seule) :
**DEAD_HOST 105 (45 %)** · NONE 81 · UNREACHABLE 17 · BOT_WALL 16 · READY 8 · NO_ADAPTER 5.
**La première cause n'est pas la détection, c'est la donnée** : sur les 105 hôtes qui ne résolvent même pas en DNS, **94 suivent le gabarit `careers.<domaine>` / `carrieres.<domaine>`** — des URLs générées mécaniquement, jamais vérifiées (NXDOMAIN confirmé indépendamment). Les maisons, elles, sont vivantes : leurs domaines racines répondent 200. Le rattrapage en repartant du **vrai domaine** en récupère ~40 % (Boardriders → SUCCESSFACTORS, Groupe Rocher → SUCCESSFACTORS sur `careers.groupe-rocher.com`, pas le `jobs.` inventé).
Deux trous de détection corrigés au passage (commit `d0f457b`) : (1) `ATS_HOSTS` filtrait les liens **avant** `detectionFromUrl`, donc Workable/Ashby/Pinpoint/Eightfold/Avature — adaptateurs existants — n'étaient jamais soumis à la détection ; (2) un ATS qu'on sait **nommer** sans savoir l'ingérer était classé « rien trouvé » : la page d'Aeropostale porte `icims` en clair et la détection renvoyait `null`. 19 vendeurs sont désormais nommés dans la note, sans jamais être promouvables. `detect.test.ts` : 19 → 32 tests.
**Conséquence de méthode** : avant de conclure « cette maison n'a pas d'ATS », vérifier que l'URL testée a été **observée**, pas fabriquée. Un gabarit d'URL n'est pas une source.

### D31 — Rebranding « Mode Careers » (logo + domaine modecareers.com)
Le produit s'affiche désormais **Mode Careers** : logo officiel SVG fourni par Loïc (2026-09-04), intégré en masque CSS `currentColor` (`public/brand/mode-careers.svg`, classe `.brand-logo`) dans le header, le menu mobile et le footer ; toutes les mentions visibles « Fashion Atlas » remplacées (titres entreprise, emails deadman/digest, `BREVO_SENDER_NAME` mis à jour sur les 3 services Railway). **Conservé volontairement : `utm_source=fashion-atlas`** sur les CTA Catwalks — le renommer casserait la continuité d'attribution premier-contact (D21) ; à renommer uniquement sur décision explicite de Loïc. Les références doctrinales D14/D18 (« Fashion Atlas by Catwalks ») restent le nom de code historique du cap stratégique. (Logo et domaine fournis par Loïc ; intégration par délégation, 2026-09-04.)

### D30 — Séquencement des actions externes (décisions Loïc, 2026-09-03 soir)
(a) **Healthchecks réglé** (période 4 h, grâce 2 h) — chaîne d'alerte externe complète. (b) **Domaine** : Loïc achète un NOUVEAU domaine (fashionatlas indisponible) ; dès qu'il me donne le nom, je câble tout par CLI (domaine custom Railway sur catwalks-web, `NEXT_PUBLIC_SITE_URL`, redirections). Jusque-là, le SEO tourne sur le domaine Railway. (c) **Google (Search Console + Indexing API service account) : DIFFÉRÉ à la fin du plan** — « seulement lorsque tous les lots sont traités à 100 % et que l'agrégateur est fiable à 100 % ». Conséquence sur la porte Lot 1 : le critère « 50 URLs au Rich Results Test » se prouve en intérim par validation structurée locale (schéma JobPosting complet, testé unitairement) + test public par échantillon ; la passe Rich Results officielle se fait avec le point (c). (d) Loïc s'offre en **renfort de recherche d'URLs ATS** : si des configs cassées résistent après le re-sweep C-05a, je lui transmets une liste précise « maison → ce qu'il faut trouver ».

### D28 — Catalogue en base (table `Source`, DEC-3) + consolidation par tenant — prise par délégation
La table `Source` (statuts DRAFT/VALIDATED/ACTIVE/PAUSED/RETIRED, config JSON, verdict robots daté, dernier run + taux desc/date/pays/url en colonnes, `tenantKey` unique) remplace `sources.csv` au runtime : ingest/orchestrateur/discovery lisent la base, `promote`/`retire-source` opèrent dessus, l'ingest **refuse** un catalogue vide, `import-sources` (idempotent, ne touche jamais `status`) tourne dans le CMD Docker entre migrate et le pipeline. **Consolidation gravée** : la contrainte tenant a révélé 17 lignes du CSV qui re-téléchargeaient le même feed groupe (Coty ×5, Condé Nast ×4, Puig ×3, Eram ×3, ELC/SMCP/L'Oréal-Avature/Beaumanoir/Clarins/L'Occitane ×2) — aucun adaptateur ne lit `brandMatch`. Supprimées ; l'attribution par marque se fait par offre (D11/A-01). Catalogue officiel : **83 sources** (vérifié : import 83/83, 0 doublon tenant). ⚠️ Lot 1 : `retire-source` sur chacune des 17 clés retirées (origins, maje-1, prada-beauty-8, morgan-2, rabanne-1, uriage, gq-france, vogue-france, vanity-fair-france, philosophy-1, rimmel, sally-hansen, vera-wang-parfums, mellow-yellow, parade-1, my-blend, melvita) — leurs offres survivent via la source de tête. Le CSV reste comme seed d'import uniquement. (Prise par délégation, 2026-09-03.)

### D29 — Cadences gravées (DEC-5) + heartbeat externe (DEC-4)
**Cadences posées sur Railway le 2026-09-03 (mutation GraphQL, relues après écriture)** : `catwalks-aggregator` cron `0 */4 * * *` (ingest toutes les 4 h), `catwalks-refresh` `30 5 * * *` (quotidien, décalé entre deux ingests), `catwalks-reconcile` `0 6 * * 1` (hebdo lundi). Avant : **aucun** cronSchedule — les 3 services tournaient en continu, relancés à chaque deploy. `PIPELINE_CMD` vérifié par service : ingest-all / refresh / reconcile (l'anomalie reconcile→ingest est corrigée en variables, effective au prochain build). **L-01 clos côté code** : invariant `staleHours ≥ ceil(pages/fenêtre) × intervalle × 1,5` testé (`cadence.test.ts` — FashionJobs : 8 runs × 4 h × 1,5 = 48 h = la fenêtre) ; l'orchestrateur enregistre un `SourceRun` **TIMEOUT/ERROR** pour toute source non terminée, et le refresh exclut BROKEN+TIMEOUT+ERROR de la clôture. **DEC-4 CLOS** : `pingHeartbeat()` en fin d'ingest-all (GET `HEALTHCHECK_PING_URL`, `/fail` si run en échec, no-op sans variable). Check healthchecks.io créé par Loïc : `https://hc-ping.com/0c4ac41d-7fc7-440a-8dfe-709a00765ac8` — variable posée sur le service catwalks-aggregator (vérifiée), premier ping succès envoyé (HTTP 200). Régler côté healthchecks : période 4 h, grâce ~2 h. **Sauvegardes Railway : IMPOSSIBLES sur le plan actuel** (mesuré à la source : `maxBackupsCount: 0` — feature Pro) ; en attendant, dump manuel restore-prouvé du 2026-09-03 dans `backups/` (hors git). (Décidé par le plan A→Z ; posé et vérifié 2026-09-03.)

### D8 — Retirer les filtres « 7 jours » et « Confirmées »
Ces deux boutons de la barre n'apportent pas de valeur candidat (« Confirmées » expose un concept interne multi-sources ; « 7 jours » isolé est bancal). Barre finale : Secteur · Contrat · Ville · Maison · Groupe · Source. (Décidé 2026-09-02.)

### D10 — Le site affiche toutes les offres (monde), avec un filtre Pays
Le site n'est plus limité à la France par défaut : il affiche **toutes** les offres actives (10 235 : monde entier), et un **filtre Pays** permet d'affiner (France, Italie, Espagne, Portugal…). `isFrance` reste stocké et sert le filtre « France » ; les autres pays viennent de `country` normalisé. Le mode « zéro filtre France » (rien jeté à l'ingestion) est conservé. (Décidé 2026-09-02.)

### D9 — Logos d'entreprises via DuckDuckGo, fallback initiale
Page Entreprises **et cartes d'offres** : logo via `https://icons.duckduckgo.com/ip3/{domaine}.ico` (gratuit, sans clé, respectueux de la vie privée). Clearbit, initialement retenu, est **mort** (racheté par HubSpot, endpoint fermé, HTTP 000) — remplacé par DuckDuckGo qui répond 200. Le domaine est deviné depuis le nom de la maison. Fallback **pastille avec initiale** si pas de domaine (`onError`). Le logo ne casse jamais l'affichage. (Décidé 2026-09-02, révisé.)

### D17 — L'UI est la DA Catwalks (noir/blanc, 400 UPPERCASE, pills), pas l'habillage Indeed
Lu à la source (skill DA Catwalks : `catwalks-backend/docs/dev-skill/references/design-tokens.md` + `components.md`). Le clone Indeed reste le modèle de **STRUCTURE/UX** (funnel, scroll page, master-detail, infinite scroll) — mais l'**HABILLAGE visuel** est la DA Catwalks, radicalement différente de ce qui a été fait (Indeed : magenta, bold, ombres, radius 12px, transition 0.2s). Règles Catwalks, non négociables :
- **Couleurs : NOIR / BLANC / GRIS uniquement — AUCUNE couleur d'accent.** Le magenta (#C622AB) mis partout est **retiré**. `--color-black #000`, `--color-white #FFF`, `--color-grey-400 #767676` (texte secondaire), `--color-bg #FAFAFA`, `--color-listing-bg #F8F8F8`, `--color-border #E1E1E1`.
- **Typo : TOUJOURS weight 400.** *« c'est la règle fondamentale »*. Le bold (700) est réservé EXCLUSIVEMENT au logo brand. Les **titres de poste sont en MAJUSCULES** (`text-transform: uppercase`), 32px sur la fiche, weight 400, letter-spacing 0.4px. Le nom de maison : 12px uppercase #767676 letter-spacing 1px. Overlines/eyebrows : 11px uppercase #767676.
- **Cartes** : radius 16px, **aucune ombre au repos**, ombre au hover seulement (`0 8px 30px rgba(0,0,0,.08)`). Hero card 32px.
- **Boutons/search/inputs** : `border-radius: 100vmax` (pill), pas 12px.
- **Transitions** : uniquement **0.3s** et 0.6s, easing `cubic-bezier(0.39,0.575,0.565,1)` (ease-lv). Pas de 0.2s (invention Indeed).
- Font `catwalks_font`, letter-spacing 0.4px, header 70px, container 1280px.

La font de marque était déjà mise (D16) ; D17 aligne **couleurs, graisses, casse, radii, ombres, transitions** sur la même DA. *Fashion Atlas porte la DA Catwalks.* (Décidé par Loïc, 2026-09-02, après lecture du skill DA à la source.)

### D16 — Typo = la font de marque Catwalks (`catwalks_font`)
Vu D14 (Fashion Atlas est une marque Catwalks, pas un clone Indeed pixel-perfect), la typo passe de Noto Sans à **`catwalks_font`** — la police propriétaire déjà utilisée par la plateforme Catwalks (`catwalks-website`), self-hébergée via `@font-face` (5 graisses : light/regular/medium/bold/oblique, woff2 dans `/public/fonts`). Le clone Indeed reste le modèle de **structure/UX** (scroll, master-detail, funnel, radii), l'**identité visuelle** est Catwalks. Noto Sans + next/font retirés. (Décidé par Loïc, 2026-09-02.)

### D15 — Fiche entreprise dédiée `/entreprise/[slug]` (corrige le clic cassé, +SEO)
Cliquer une maison menait vers `/?maison=` — cassé (route `/` = landing depuis D14, plus les résultats) et sans vraie fiche. Indeed a une **fiche entreprise riche** (`/cmp/Veolia` : logo, note, avis, salaires, PDG, effectifs, CA, secteur, offres…) — mais ces données (avis, salaires agrégés, dirigeants) **on ne les a PAS**. Décision : construire une **page `/entreprise/[slug]`** avec ce qu'on a réellement — logo, nom, secteur, groupe (`parentGroup`), nb d'offres ouvertes, villes, + la **liste de ses offres** (master-detail comme `/emplois`), description/lien carrière si dispo. Corrige le bug ET crée **~515 pages indexables** (levier SEO majeur, D14) : `/entreprise/chanel`, `/entreprise/dior`… La grille Entreprises et les liens maison pointent vers cette page. (Décidé par Loïc, 2026-09-02.)

### D18 — Le pont candidature : DOUBLE bouton, sans jamais promettre une transmission
La tension réelle (tranchée avec Loïc) : Fashion Atlas agrège des offres publiques d'employeurs (Cartier, Dior…) qui ne sont PAS clients Catwalks. Donc « Postuler » ne peut PAS promettre « on transmet ta candidature à Cartier » — ce serait une **promesse non tenue = tromper le candidat** (interdit par la doctrine). Le modèle honnête retenu = **deux boutons distincts** sur le détail d'offre :
1. **« Postuler chez [Maison] »** → lien DIRECT vers l'employeur (`job.url`), ce que Fashion Atlas fait déjà. Valeur agrégateur, honnête : l'offre Cartier mène chez Cartier.
2. **« Créer mon profil Catwalks »** → `https://catwalks.io/inscription?utm_source=fashion-atlas&utm_medium=aggregator` → onboarding Catwalks (profil + CV, D-134) → matching avec les VRAIES Maisons clientes de Catwalks + « X offres qui matchent ton profil ». Le message ne promet jamais de transmettre à l'employeur de l'offre agrégée ; il promet un **matching actif** avec l'écosystème Catwalks — ce que Catwalks tient réellement.

**Vérifié à la source (catwalks-website)** : `/inscription` existe (page canonique, `AuthForm`, D-134), l'inscription mène à `/onboarding` puis `/mes-jobs`, et le concept d'« offre d'origine » (retour après inscription) existe déjà côté Catwalks. Le pont côté Fashion Atlas = juste les deux CTA + l'URL UTM ; rien à construire côté Catwalks. Domaine Catwalks : **catwalks.io**. Funnel complet : *Agrégateur → référencement SEO → le candidat veut postuler → double choix (direct employeur OU profil Catwalks) → matching Catwalks*. (Décidé par Loïc, 2026-09-02.)

### D14 — Deux marques : « Fashion Atlas by Catwalks » (search) → « Catwalks » (candidat)
Cap stratégique validé par Loïc (2026-09-02). Cet agrégateur devient **Fashion Atlas by Catwalks** : le moteur de recherche gratuit et exhaustif des offres Mode·Luxe·Beauté·Retail (SEO, acquisition, longue traîne — `/jobs/chanel`, `/jobs/paris`, `/companies/dior`…). Sa fonction économique n'est PAS de monétiser directement : c'est **d'alimenter Catwalks en candidats à grande échelle**. **Catwalks** (plateforme distincte, **déjà existante** — dossier à intégrer au workspace) possède le candidat : compte, CV, candidature, matching, et la monétisation B2B côté Maisons (CVthèque, matching, publication premium, ATS léger, abonnement). Résumé : *Fashion Atlas owns the search, Catwalks owns the candidate.*

**Le pont** : Google → Fashion Atlas → offre → « Apply with Catwalks » → **candidature progressive** (email/Google/LinkedIn + upload CV + Apply = compte Catwalks créé automatiquement, PAS d'onboarding de 5 min qui tue la conversion) → « ✅ Application sent · complete your profile » → enrichissement progressif → matching (« 37 other jobs match your profile »). Le candidat ne doit quasiment pas sentir le changement de plateforme.

**Moat** : 30K+ offres indexées + dizaines de milliers de pages SEO + comptes candidats + données comportementales + CV + matching + relations recruteurs — beaucoup plus dur à copier qu'un simple jobboard.

**Impact à cadrer avec Loïc AVANT d'implémenter** (aucune de ces décisions n'est prise seul) : (a) le **nom/branding** du site passe de « Catwalks » à « Fashion Atlas by Catwalks » ; (b) le **domaine** change (avant le SEO) ; (c) la **font** : Noto Sans a été mise pour cloner Indeed — une identité Fashion Atlas propre peut la remplacer, ce qui assume un écart au clone Indeed strict ; (d) le **pont candidature** exige de lire le code réel de Catwalks (à la source) avant conception. (Décidé — cap ; implémentation à cadrer.)

### D13 — Clone Indeed : la PAGE scrolle (pas des panneaux figés)
Objectif validé par Loïc : **clone Indeed**, pas « inspiré ». Mesuré à la source (Playwright sur fr.indeed.com) : Indeed est une **page web qui scrolle naturellement** — `body overflow: visible`, header `static` qui part vers le haut au scroll, **barre de filtres `fixed`** qui reste collée, détail à droite `sticky`. Notre layout actuel est au contraire une **app à hauteur fixe** (`h-dvh`, `overflow-hidden`, header figé, 2 panneaux qui scrollent isolément) — c'est le modèle Gmail, pas Indeed. Décision : **refondre le layout `/emplois` en page scrollable** : plus de `h-dvh`/`overflow-hidden` sur le conteneur, le body scrolle, le header défile avec, la barre de recherche+filtres devient `sticky`/`fixed` en haut, la liste défile, le détail est `sticky` à droite. Mesures Indeed de référence conservées pour valider le comportement de scroll réel, pas seulement les tailles statiques. (Décidé et validé par Loïc, 2026-09-02.)

### D12 — Le site est le modèle INDEED, FR par défaut, sans carte
Le référentiel visuel est **Indeed** (fr.indeed.com), pixel-perfect. (a) **Offres** : liste à gauche + **détail de l'offre à droite** (master-detail), cartes fines et denses SANS logo (le titre mène le scan), auto-sélection de la première offre. (b) **Entreprises** : le modèle Indeed « Meilleures entreprises » (fr.indeed.com/companies) — une **grille de maisons** (logo, nom, secteur, nb d'offres = « Emplois ouverts »), **SANS carte géographique**, cliquer une maison → ses offres. (c) **Périmètre : FR par défaut** — le site montre la France (`isFrance:true`) par défaut, un **filtre Pays** permet d'élargir au monde. La donnée monde reste stockée (SEO/B2B futur), simplement pas affichée par défaut. **Aucune carte** nulle part (Indeed n'en a pas) : le composant JobMap/WorldMap et la mini-carte du détail sont retirés. (Décidé et validé par Loïc après comparaison Playwright Indeed-vs-nous, 2026-09-02. Révise D10 : FR par défaut au lieu de monde par défaut.)

### D11 — Une offre de groupe est créditée à la MARQUE de tête, pas au groupe
Les flux ATS de groupe (L'Oréal Luxe, Puig, Richemont, SMCP…) étiquettent chaque offre « marque de tête +N » (« Cartier +3 », « Dr. Jart+ +13 »), où N compte les autres maisons du flux. Stocké tel quel, ce compteur devient un **nom de société fantôme** que le candidat voit sur des milliers d'offres. Décision : on affiche **la marque** (Cartier, Helena Rubinstein), pas le groupe (Richemont) — _un candidat postule chez Cartier, personne ne cherche « un job chez Richemont »_. Le groupe reste accessible via le **filtre Groupe** (`parentGroup`). Implémenté par `stripMultiBrandSuffix` (retire un « +N » final, préserve le « + » d'un vrai nom comme « Dr. Jart+ ») + `resolveCompany` qui mappe la marque connue vers son orthographe canonique. **Auto-guérit au prochain ingest** ; ~4 900 offres (16 sociétés/151) restent mal étiquetées jusqu'au déploiement. (Décidé et validé par Loïc, 2026-09-02.)

## État vérifié de la prod (2026-09-02) — ce qui NE va pas

Constats lus dans la base et les logs de prod, pas supposés :

1. **La base ne contient que 3 employeurs** (Kering, L'Oréal, Groupe Courir) pour **2 140 offres**, dont **305 seulement en France**. Très loin du marché visé (728 maisons, 93 flux annoncés).
2. **L'ingest meurt avant d'atteindre les flux API.** `runIngest` fait d'abord les sources sitemap (8), puis les flux API (le gros du marché) — mais le run s'arrête pendant les sitemaps (timeout / container tué). Les 22 adaptateurs ATS ne produisent donc **rien en prod**.
3. **L'Oréal = 0 offre France** sur 1 662 offres (bug de parsing de localisation Avature).
4. **WTTJ tronqué** : 59 560 offres à la source, 17 lues, 12 en France.
5. **Puig = 0 offre** (source cassée, silencieuse).
6. **La purge par génération s'exécute AVANT tout fetch, hors transaction, sans garde de succès** → si le fetch échoue après un bump de version, la base reste vide.
7. **`pipelineVersion` n'est écrit qu'au CREATE** → les offres MERGED/attach gardent l'ancienne version et sont re-supprimées au run suivant (churn : `firstSeenAt` et `id` instables). Preuve : 31 offres encore en v4, dont 12 multi-sources.
8. **`SourceRun` est vide** → le monitoring de santé n'a jamais rien enregistré ; l'ingest meurt avant `checkSourceHealth`.
9. **La santé (`health.ts`) se compare à elle-même** (lit `JobSource` live après l'ingest) et n'est **pas** consultée par `refresh` → une source cassée voit ses offres fermées 48 h plus tard sans garde-fou.
10. **`catwalks-reconcile` lance `ingest`** au lieu de `reconcile`.
11. **Zéro test** dans tout le dépôt.

Détail exhaustif des findings (3 audits défensifs, chaque regex exécutée) : voir `AUDIT.md` (à produire).

## Sécurité — points ouverts

- **XSS JSON-LD** : `apps/web/app/offre/[id]/page.tsx` injecte `JSON.stringify(structuredData)` dans un `<script>` sans échapper `</script>` → titre/description d'une source hostile peut casser hors du script.
- **SSRF** : `lib/http.ts` / `ats/detect.ts` fetchent des URLs issues de Serper / HTML / CSV sans valider le schéma ni bloquer les hôtes internes.
- **Clé Algolia LVMH** codée en dur (`lvmhAlgolia.ts`) — clé publique de secours, à documenter comme telle.

## Tests

- **Unitaires** (sans base) : `npm run test:unit -w @catwalks/aggregator` — normalizers, france, html, ssrf, geocode.
- **Intégration** (base dédiée) : `DATABASE_URL=…/catwalks_test npm run test:integration -w @catwalks/aggregator` — purge, santé, refresh.
- ⚠️ **Les tests d'intégration VIDENT la base.** Ils ne tournent QUE sur une base dont le nom contient « test » (garde `src/test/setup-integration.ts` qui refuse sinon). **Ne jamais** les lancer sur la base de démo/prod. Base de démo locale = `catwalks`, base de test = `catwalks_test` (même conteneur Docker `catwalks-audit-pg`, port 55440).

## Règles git — toutes les sessions, sans exception (décidé par Loïc, 2026-09-03)

> Contexte : plusieurs sessions Claude travaillent en parallèle dans le MÊME
> working tree. Un `git reset --hard` d'une session a effacé le travail non
> commité d'une autre. Et Railway auto-déploie `origin/main` à chaque push.

- **Interdits** : `git reset --hard`, `git checkout -- .`, `git clean`,
  `git stash drop`, `git push --force` sur toute branche partagée. En cas de
  besoin réel : `git stash push -m "<session> <raison>"` et on en parle.
- **`git status` obligatoire avant tout changement de branche.** Si des
  fichiers modifiés ne sont pas à toi, tu ne bouges pas (préférer
  `git worktree add` pour travailler sur une autre branche sans toucher le
  tree partagé).
- **Une session = un périmètre de fichiers.** Web : `apps/web`. Pipeline :
  `apps/aggregator`, `packages/db`, `data/`. Aucun commit hors périmètre ;
  fichiers racine partagés (CLAUDE.md…) : prévenir l'autre session d'abord.
- **Push à chaque fin d'étape.** Jamais de travail local non poussé de plus
  d'une heure.
- **`main` = prod (Railway auto-déploie chaque push).** Pousser sur main =
  déployer : on le fait en le sachant, jamais par accident.

## Méthode de travail

1. **Audit défensif** (fait, 3 passes parallèles + vérif prod).
2. **Boucle obligatoire par correctif** : reproduction (test/BDD) → correction → audit défensif → tests de non-régression → validation. Après chaque correction : audit défensif / BDD → validation → correction suivante.
3. **BDD** : formaliser les comportements attendus en scénarios vérifiables (socle de tests à créer — aujourd'hui absent).
4. Rien n'est « corrigé » parce que ça paraît logique : **preuve d'exécution** exigée.

### D34 — Doublon ELC / Dr. Jart : 1 332 postes affichés deux fois, retrait de `dr-jart-13`
Mesuré le 2026-09-06 : `dr-jart-13` (origin `careers.elcompanies.com`) et `estee-lauder-companies` (origin `elcompanies.eightfold.ai`) sont **le même tenant Eightfold** (`domain=elcompanies.com`) — 1 462 externalId identiques, feed de 1 441 offres visité deux fois par run (10 min chacune, sur un run de 3 h 28 pour un cron de 4 h). La garde tenant ne l'a pas vu : `domain` n'était pas dans le locator, la clé tombait sur l'origin (corrigé, commit `701813e`).
Le doublon est **visible par le candidat** : 1 332 postes existent sous deux Jobs, parce que Dr. Jart étiquette 1 319 offres « Dr.Jart+ » là où ELC attribue correctement (MAC 379, Jo Malone 130, Clinique 115, Tom Ford 81…). C'est le risque A-01 réalisé, pas un défaut de dédup : le reconcile ne peut pas fusionner deux sociétés différentes.
**Décision (par délégation)** : `retire-source dr-jart-13`. Preuve préalable exigée par D27 : 1 462/1 502 externalId présents chez ELC avec la bonne marque ; les 40 restants sont périmés (ELC = 1 462 au total). Rien de vivant n'est perdu. Exécuté dans la fenêtre entre deux runs, jamais pendant un ingest.
> **Exécuté le 2026-09-06 à 07:27 UTC**, après la fin du run de 04:00 et avant tout autre run : 1 502 JobSource détachées, **1 360 offres « Dr.Jart+ » supprimées** (leur jumelle ELC existe sous la bonne marque), 142 offres multi-sources conservées, 141 URL canoniques réassignées.

### D35 — Crons gelés, orchestrateur parallèle, lot « GENERIC » : 23 portails de plus (2026-09-06)
**Décision Loïc** : « après ce dernier run, il faut stopper tous les runs, pour se concentrer sur les fix » — un run durait **3 h 28 pour un cron de 4 h** (chronométré dans les logs : 470 sources en SÉRIE, 76 % du temps sur 15 sources qui attendaient leur hôte pendant que les 455 autres attendaient aussi). Fait :
- **Les 3 crons Railway sont gelés** (`0 0 29 2 *`, prochain tick 2028-02-29 ; Railway refuse « 30 février »). Le refresh est gelé AVEC l'ingest : après 48 h sans ingest il fermerait toutes les offres. Vérifié : la mutation ne redéploie pas et ne tue pas un run en cours ; un push déploie sans lancer de run. Un run se déclenche à la main (cron posé à la minute voulue). **À la reprise : ingest complet d'abord, refresh ensuite.**
- **Orchestrateur parallèle** : 4 sources à la fois (`INGEST_SOURCE_CONCURRENCY`), ordre petites-d'abord conservé ; la porte par hôte (D25) garde la politesse par hôte, donc paralléliser des sources ne change rien au débit par hôte. Geocode retiré du chemin par source (une passe en fin de run). Durée attendue ≈ 1 h, à mesurer au premier run manuel.
- **Zéro muet corrigé** : Michael Page alternait 2 936 offres et « 0 fetched, 0 errors » un run sur deux (pages de détail vides sous challenge Cloudflare, chaque échec avalé). Un échec total des pages de détail est désormais une erreur nommée avec compteurs.
- **Lot GENERIC** : sur les 26 portails du fichier « Global Direct Job Portals » que le crawler générique lisait à 0–10 offres, cinq investigations parallèles (rapports `data/g1..g5-rapport.md`), **chaque conclusion re-mesurée par exécution** avant intégration. Résultat : 12 adaptateurs (Oracle HCM, Taleo TBE, Altamira, Jobylon, Jibe, Volcanic, Talent Funnel, Eqwa, GeoDirectory, Typesense, Rituals, Ba&sh) + chemin **SAP RMK v2 en JSON** dans SuccessFactors (Douglas 0 → 130, Breitling 0 → 41) + deux correctifs transverses (« - » n'est pas une ville : Boots ; index sitemap mal déclaré : Selfridges). 23 candidats passés par les trois portes (`addFoundSources`). Décisions Loïc : **YNAP seul, pas Mytheresa** (sous-ensemble strict, cas D34) ; Tom Ford est déjà dans le flux ELC ; ASOS déjà actif via SmartRecruiters (adaptateur Algolia retiré, D28). Restent : PVH (1 339) et Ralph Lauren (1 085) derrière un WAF Amazon — amorçage navigateur en cours comme brique de transport (D25) ; APM Monaco vide.
- **Règle de méthode gravée** : un rapport d'agent est une piste, jamais une conclusion — chaque chiffre a été rejoué par une exécution propre avant d'entrer au catalogue ou dans ce fichier.
### D36 — Runs ciblés jusqu'à 100 %, puis crons une fois par jour, la nuit (2026-09-06)
**Décision Loïc** : « on arrête vraiment les runs trop longs : une solution localisée pour ajuster ; un run global seulement quand on est sûrs à 100 % ; ensuite réactiver proprement les crons, une fois par jour max, idéalement 2–3 h du matin, pour détecter et nettoyer les offres périmées ». Fait :
- **Run ciblé** : `INGEST_ONLY_KEYS="hermes,kering"` sur le service Railway + cron posé à la minute = un run de quelques minutes sur les seules clés listées, depuis l'egress de la prod (D32). En local : `DATABASE_URL=<prod> npx tsx src/cli.ts ingest --only <clé>` (egress différent, suffisant pour un adaptateur ou une normalisation).
- **Cadence** : `INGEST_INTERVAL_HOURS` 4 → 24 ; à la reprise, ingest `0 1 * * *` (03:00 Paris en été), refresh `0 4 * * *`, reconcile hebdo. FashionJobs couvre tout le board en un run (fenêtre 300 pages) pour tenir L-01 (36 h ≤ 48 h). **Les crons restent gelés** (`0 0 29 2 *`) jusqu'au run global validé.
- **Ordre à la reprise** : run global manuel → vérification → ingest cron → refresh cron (jamais le refresh avant un ingest complet).
- **Logos** (revue Loïc) : `Company.domain` lu à une source qui NOMME la Maison (domaine carrière du catalogue, sinon Wikidata P856, sinon initiale — jamais un logo deviné depuis le nom : mac.com, omega.com étaient d'autres entreprises). Mesuré : 36/40 Maisons, Louis Vuitton → louisvuitton.com (pas LVMH). Migration `Company.domain` appliquée à la main en prod AVANT le push (crons gelés = pas de migrate au démarrage ; le web lit la colonne → 503 sinon).
- **Descriptions** (12 sources DEGRADED préexistantes) : Hermès et Diptyque (WTTJ : le texte n'est plus dans l'index Algolia, seulement sur l'API par offre), L'Oréal (JSON-LD = titre + date, texte en microdata), Kering (JSON-LD = portrait de la Maison, poste dans `__NEXT_DATA__`), Sephora France (URL de détail doublée), adidas/Avolta/L'Oréal Pro (structure écrasée par le parseur microdata partagé), L'Oréal Pro (carte coupée sur `</a>`). Jako : vide à la source, DEGRADED juste.
- **Doublons de routes** (cas D34 répété) : `loreal` sitemap (1 788 offres sans lieu ni description) doublait `l-oreal-professionnel` (1 767 ids communs), et « L'Oréal Professionnel » était une étiquette fausse du portail groupe → route retirée du registre + `retire-source loreal` + fusion d'identité vers « L'Oréal » (ids, URL, firstSeenAt conservés) ; Kering : route sitemap (1 427 dont 394 périmées, un stage de 2021) doublait le flux Eightfold sous la MÊME clé → `retire-source kering --external-prefix=https://` (nouvelle option, testée). « VanCleef-Aprels » (faute du flux Richemont, 266 offres) → alias Van Cleef & Arpels + fusion.
- **Web** (revue Loïc) : nouveau logo (SVG aplati, le `<mask>` Figma ne passe pas en masque CSS), hero « N offres. N Maisons. Un seul moteur. », liseré blanc du hero retiré, filtres aérés.

### D37 — Cinq audits défensifs (2026-09-06, après-midi) : ce qu'ils ont prouvé et ce qui est corrigé
Lancés à la demande de Loïc (canonicité, architecture, cadence, informations des offres, liens morts ; rapports `data/audit-a1..a5-*.md`, chaque constat avec sa preuve exécutée). **Règle** : pas de rapport, un plan par lot, et l'exécution ; un run global seulement quand tout est vert.
- **Architecture (A2)** — corrigé : iCIMS jamais planifié (URBN 1 329 + 906, Aéropostale : le kind manquait dans `KIND_TO_ATS` ; le dispatch est une table exportée testée contre la table des kinds) ; **1 855 offres vivantes à JobSource inactive** (la récupération P2002 touchait le Job, pas la JobSource : fermées par le refresh puis ré-ouvertes, chaque jour) → identité exacte (sourceKey + externalId, active ou fermée) cherchée AVANT le cluster, toute ré-attestation passe par un seul chemin (Job + JobSource + clé de cluster) ; clé de cluster sans la ville structurée (4 158 offres) ; « 0 après BROKEN » enregistré OK (66 runs) → référence = dernier run productif ; 404 rejoué 3× → `HttpStatusError` ; **registre codé en dur supprimé** (six routes sitemap doublant des sources de la table ou tournant à vide : Courir 376, Galeries Lafayette 168, Lacoste 98 doublons ; Decathlon 316, WTTJ, Puig retirés).
- **Canonicité (A1)** — corrigé : pays déduit du lieu (14 074 offres sans pays, 18 %), liste ISO complète (Lettonie, Serbie, Kosovo refusés avant), `isFrance` cohérent avec le pays normalisé ; ville jamais un pays (~800) ; lieu « /a> » et scripts refusés ; titres nettoyés (2 400) ; `&amp;amp;` (2 515) ; salaires > 1 M en devise majeure (Michael Page 58 M€) ; dates futures (2028) ; période et télétravail canoniques ; groupe de repli depuis l'alias (~5 100). **Lot 4 livré** (`data/l4-rapport.md`) : ville canonique par une chaîne unique segments → rejet → exonyme → casse, partagée par la clé de dédup et l'affichage ; tables lisibles `data/villes-exonymes.csv` (485 : Londres, Milan, Genève, Munich, Lisbonne…) et `data/villes-non-lieux.csv` (361 : régions, états, modes, services) ; 223 cas réels testés ; simulé sur l'export prod : **8 047 → 6 500 villes distinctes**, 10 458 offres renommées, 3 434 faux lieux retirés (pays, états, codes magasin), 702 villes récupérées depuis le lieu. Choix gravés : Guangzhou (pas « Canton », 32 Canton US), Venice/Vienna/Warsaw/Moscow traduits seulement si le pays est dans le libellé (homonymes américains), « Mexico City », villes CJK inconnues gardées telles quelles. Suffixes juridiques et entités locales retirés de l'identité de Maison (« Ulta Beauty, Inc. », « Coach Stores Canada Corporation » → Coach). « Logo » et sociétés périmées migrent par la ré-attestation de société.
- **Liens morts (A5)** — mesuré : ≈ 330 offres mortes encore affichées (0,45 %) mais **3 320 liens « Postuler » cassés sur des offres vivantes** → corrigé : Estée Lauder (125 → 2 : l'URL vient de l'id de la position), normal (478 → 25 : `config.jobOrigin` jobs.normal.fr), Foot Locker (2 842 → 10 : `/login` → `/job`) ; **3 128 offres au rang canonique orphelin** bloquaient tout rafraîchissement d'URL → le rang n'a d'autorité que porté par une source attachée ; **le 410 ne sortait jamais en prod** (deux causes prouvées : la sonde du middleware dérivait de `request.url` = `https://0.0.0.0:8080` derrière le proxy ; puis le serveur standalone écoutait sur le nom du conteneur, pas sur la boucle locale) → sonde sur `http://127.0.0.1:PORT` + **`HOSTNAME=0.0.0.0` posé sur `catwalks-web`** (à ne jamais retirer) ; la sonde loggue son échec. **Vérifié en prod le 2026-09-06 12:19 UTC : offre fermée → 410 + `x-robots-tag: noindex`, active → 200, inexistante → 404.**
- **Informations des offres (A4)** — corrigé : la ré-attestation ré-écrit désormais date, langue, contrat, temps, validité, salaire, télétravail et société (lignes nées avant le 04/09 : 38 % sans date, 80 % sans langue) ; **validité JSON-LD** : 21 157 offres (29 %) publiaient une validité dépassée → tant que l'ATS liste l'offre, horizon aujourd'hui + 30 j (décision Loïc : « si l'offre est encore dans l'ATS, on met à jour ») ; filtre sectoriel sur les jobboards (Michael Page : 97,7 % hors secteur) ; Mosaic, Make, Stone by Stone retirés (hors secteur, accord Loïc). **Le FR en dur du JSON-LD signalé plus haut n'existe plus** (vérifié). **Lot 2 livré et rejoué** (`data/l2-rapport.md`) : Workday détail en anglais (« Entraîneur » → « Coach », pays ISO 98/100) + `timeType` (temps 0 → 98/100) ; `FULL_TIME`/`PART_TIME`/Teilzeit/全职 reconnus, Regular → CDI, Temporary/Seasonal/Fixed term → CDD ; contrat/temps depuis les champs structurés : Ulta temps 0 → 9 868, Foot Locker contrat 26 → 2 779, H&M 0 → 508, ELC/Kering 0 → 75/76 ; pays Estée Lauder 0 → 1 448 (chaînes « City, Region, CC ») ; langue LVMH 0 → 5 490, H&M 0 → 1 000 ; dates L'Oréal (mode liste) et Taleo (0 → 18) ; **Kering crédite la Maison** (Gucci, Balenciaga, Saint Laurent… via `efcustomTextHouse`) ; Foot Locker = « Foot Locker » (l'enseigne Champs Sports / Kids Foot Locker est opt-in par `config.brandTag` — **décision Loïc**). H&M plafonné à 1 000/1 622 par une borne codée en dur : levée (l'API sert au-delà, vérifié).
- **Cadence (A3, partiel)** — run 1 : 61 min de mur, 235 min de temps-source sur 4 voies (96 % de saturation) ; routes sitemap 29 % du temps (retirées depuis) ; budget par source 20 → 40 min (Michael Page ne lisait que 1 450/3 334). **Michael Page retiré** (décision par délégation, lot 3 confié par Loïc) : 76 offres en périmètre sur 3 334, ~19 min de crawl par run, zéros intermittents sous challenge Cloudflare, re-ingest local bloqué 36 min → `retire-source` (3 964 rattachements, 3 964 offres, aucune partagée). Réversible : la ligne Source existe en RETIRED. **Décisions ouvertes (Loïc)** : Foot Locker par employeur (actuel) ou par enseigne (`config.brandTag`) ; LVMH dont 2 628 liens mènent au formulaire officiel de candidature (gardé).
- **Ordre de reprise gravé** : run global manuel → vérification → ingest cron → refresh cron (le refresh avant un ingest complet fermerait ~1 850 offres vivantes).
> **Run global de validation (2026-09-06, 13:11:42 → 14:15:34, 1 h 04) : 488/488 OK, 0 échec.** Indicateurs mesurés en base, avant (06:26, avant tout correctif) → après : offres actives 56 455 → 72 029 ; **sans ville 37 845 → 1 765** ; ville récupérable 31 627 → 789 ; **secteur hors référentiel 22 015 → 16** ; « Apply Now » 184 → 8 ; France cohérente (`isFrance` = pays FR) 5 859 → 8 564 sur 8 661 ; pavés sans saut 6 827 → 3 464 (dont Foot Locker 1 006, plat à l'API) ; descriptions avec HTML 5 712 → 2 238, **0 sur les lignes ré-attestées** ; offres vivantes sans rattachement actif 1 855 → 89 ; pays non ISO restants : 100 % sur les 3 829 lignes NON ré-attestées par ce run (19 sources BROKEN par deux régressions du jour, corrigées : filtre sectoriel devenu opt-in, `jobOffset` Avature remplacé — rejouées par run ciblé Railway). La ré-attestation guérit la base : ce qui reste sale est ce qui n'a pas été re-vu.
> **Run ciblé Railway sur les 19 sources (14:24 → 14:33, 9 min)** : 19/19 OK, L'Oréal 1 711 offres à 100 % de description. **Refresh manuel (14:41)** : 73 014 vérifiées, **504 fermées** (exactement l'estimation : aucune source ne les re-listait depuis 48 h, FashionJobs 178, L'Oréal 32, LVMH 29, Nike 26), 0 ré-ouverte ; cron re-gelé. Le pipeline est prêt pour la reprise des crons (D36) : ingest `0 1 * * *` (03:00 Paris), refresh `0 4 * * *`, reconcile `0 5 * * 1` — **à activer sur le go de Loïc**.

### D38 — Catwalks Intelligence : l'observatoire, et l'historique commence AUJOURD'HUI (2026-09-06)
**Décision Loïc** (brief `intelligence_business.md`) : construire **Catwalks Intelligence**, l'observatoire mondial du recrutement Mode · Luxe · Beauté · Horlogerie · Retail — pas une page « chiffres » — avec sa DA (`design_2.md`) et des graphiques « ultra clean », bien référencé. Décisions techniques prises par délégation, dans l'ordre d'importance :
- **Historisation dès maintenant** : `Job.closedAt` / `reopenedCount` (posés par le refresh), table `JobEvent` (OPENED / CLOSED / REOPENED / CHANGED sur titre, ville, pays, société, métier), table `MarketSnapshot` (photographie quotidienne par périmètre : global, pays, ville, Maison, groupe, secteur, métier, famille, séniorité, contrat, IA, pays×métier, pays×secteur ; écrite par la commande `snapshot`, enchaînée après le refresh). Les comparaisons J-7 / J-30 / M-12 lisent les snapshots, jamais Job. **Vérité affichée** : l'historique commence le 2026-09-06 ; toute fenêtre plus ancienne rend « n/d », jamais un chiffre reconstruit présenté comme observé (avant le 04/09 la base était reconstruite à chaque run).
- **Taxonomie du secteur à l'écriture** (`normalize/taxonomy.ts`, `skills.ts`) : 25 familles de métier (Conseil de vente, Direction de boutique, Visual merchandising, Atelier & savoir-faire, CRM & clienteling…), 9 niveaux de séniorité, familles retail / atelier / corporate, drapeau IA, compétences par dictionnaire fermé (outils, langues près d'un mot de langue seulement, concepts, certifications). Classée à la création, re-classée à la ré-attestation (`taxonomyVersion`), rejouée sur toute la base par `classify-jobs`. Mesuré sur 12 000 offres prod : 86 % classées ; le drapeau IA passait par les clauses « n'utilisez pas ChatGPT en entretien » (18 % d'offres « IA ») → phrases de processus retirées avant test.
- **FACT / DERIVED / INSIGHT** séparés partout, couverture et méthodologie affichées, seuils minimaux (30 offres, 2 snapshots) sous lesquels on dit « n/d ».
- **Hors périmètre de ce lot** (à cadrer avec Loïc) : Talent Supply & Demand et Talent Pressure (base candidats Catwalks, autre produit), salaires (volume insuffisant : mesurer d'abord), Ask Catwalks (couche conversationnelle), gating Enterprise. Le reste (pages, indices, radar, rapports, insights en langage naturel par règles) est dans les lots W1/W2.
- Migration `20260906170000_intelligence_history` appliquée en prod à la main avant le push (crons gelés, cf. mémoire).

> **Audit défensif avant déploiement (2026-09-06 soir, demande Loïc : « la promesse tenue à 100 % »)** — cinq axes (`data/audit-i-*.md`), chaque constat prouvé par exécution, tout corrigé avant le push :
> - **Chiffres** : ~320 nombres recalculés en SQL brut, exacts sauf trois — « Pays » d'une Maison plafonné à 15 par une limite d'affichage (Cartier 33), repli pays fragile (Türkiye/Czechia selon la version d'Intl), Maisons par pays additionnées sur les graphies. Corrigés ; en prod, 509 offres contredisaient isFrance/pays et ~300 pays étaient en graphie libre (lignes jamais ré-attestées) → remises en cohérence (0 contradiction) et règles gravées à l'écriture (un lieu reconnu français sans pays → FR ; « US-LA-New Orleans » n'est pas la France).
> - **Historisation** : rejouer un jour de snapshot ré-écrivait l'histoire depuis l'état actuel des offres → colonne `MarketSnapshot.mode`, un jour `live` n'est jamais remplacé par une reconstruction, fermetures comptées depuis les événements CLOSED (immuables), perdant d'un reconcile daté et tracé MERGED, pas de photographie sur un refresh refusé ; 1 478 fermetures d'avant D38 datées.
> - **Taxonomie v2** : 2 × 300 offres jugées une par une — 4 357 coiffeurs Ulta en « Création & design », « Selling Associate » sellier, « Machine Learning Engineer » RH, coordinateurs managers, « Sales Associate » junior ; indice IA à 47 % de faux positifs (clauses d'entreprise, code de l'Iowa). Règles réécrites (266 tests), **décisions par délégation** : « Specialist / Expert » et coordinateur = confirmé (MID), « Sales Associate » = confirmé, « General Manager » d'un magasin = directeur de boutique, garde IA par société (> 80 % d'une société = texte d'entreprise) chaque nuit. **Onze sources hors secteur retirées** (Stripe Inc., Toast, Infuse, Bentley Systems, Tulip, Efore, Ohme, Towa, Dept, Wing, Peloton : 1 921 offres sous des noms de marques — homonymes de slug, cf. mémoire).
> - **SEO / perf / sécurité** : aucun critique ; licence CC BY 4.0 publiée dans le JSON-LD sans décision → **retirée** (à Loïc de décider d'une licence de données) ; slugs validés avant la base et index d'entités mémorisé (plus d'entrée de cache par slug inconnu) ; `landingStats` mémorisé 10 min (10 requêtes par page vue, tout le site) ; sitemap avec Cache-Control.
> - **DA / vérité d'affichage** : le séparateur de milliers fr-FR (U+202F) n'est pas dessiné par la police display — « 71525 » sur le hero en prod → U+00A0 partout ; « Nouvelles 24 h / 7 j / 30 j / 90 j » n'affichent un chiffre que lorsque l'observation fiable couvre la fenêtre — **`OBSERVATION_START = 2026-09-07`**, lendemain de la consolidation du catalogue (mesuré après le premier déploiement : « 32 600 nouvelles en 24 h » = entrée des sources ajoutées le jour même, pas le marché) — sinon « disponible à partir du … » ; blocs à parts étiquetés « Fait observé · parts dérivées » ; « En direct » retiré du H1 (crons gelés) ; part de marché seuillée sur le périmètre ; **indices, variations, momentum et intensité ne lisent que les photographies `live`** (l'indice de Cartier affichait 457,6 sur une base reconstruite du 4 septembre) ; les lignes « Autres » des secteurs fusionnées.
> - **Reste ouvert (décisions Loïc)** : licence de publication des données de l'observatoire (aucune n'est affichée) ; lot W2 (radar, « ce qui a changé cette semaine », rapports hebdomadaires, compétences) une fois 7 jours de photographies en direct ; reprise des crons (`0 1 * * *` ingest, `0 4 * * *` refresh + snapshot) — sans eux, aucune photographie quotidienne.

### D39 — Foot Locker par enseigne, WTTJ par balayage sectoriel (2026-09-06)
- **Foot Locker** : crédité par enseigne (D11 : la marque, pas le groupe) — `brandTag: "tags4"` sur la ligne Source ET dans `sources.csv` (import-sources ré-écrit la config au démarrage). Mesuré après run ciblé : Foot Locker 1 922 · Champs Sports 482 · Kids Foot Locker 429 · WSS 8.
- **WTTJ** : la route sitemap (morte derrière le WAF Amazon) est remplacée par le **balayage sectoriel par l'API** (`wttjSector.ts`, kind `wttj-sector`, même AtsType WTTJ = même identité d'offre) : facettes `luxury-1`, `fashion-1`, `cosmetics`, `jewelry-1`, `mode` → 138 organisations, **2 091 offres en 168 s**, rejoué deux fois. Exclusions à la config : 33 organisations classées « Luxe »/« Cosmétiques » par WTTJ mais hors Maisons (Ritz Paris, Potel & Chabot, dermo-pharma, bien-être, vins, Devialet, agences). Trois homonymes de slug retirés (`frame` cabinet de conseil, `corum` fintech, `bedrock` média : 11 offres hors secteur affichées sous des noms de Maisons). Les sources `wttj` par société couvertes par le balayage sont retirées après le premier run (sinon relues deux fois).
- **LVMH** : les liens vers le formulaire officiel de candidature sont conservés (décision Loïc).

> **Premier run manuel avec tout ça (2026-09-06, 07:59:47 → 09:01:24)** : **492/492 sources OK, 0 échec, 1 h 02** (contre 3 h 28 la veille pour 470), heartbeat envoyé ; **76 098 offres actives** (56 572 avant). Dédup vérifiée sur Tiffany : 419 lues → 14 créées, 405 fusionnées avec les offres LVMH existantes. Trois enseignements mesurés ce run-là : (1) **les normalisations d'écriture ne touchaient que la création** — pays distincts 276 → 282, sans ville 37 845 → 37 770, « Apply Now » 184 → 184 : les 56 000 lignes existantes n'étaient jamais ré-écrites → corrigé (`reattestationFields` : une ré-attestation ré-écrit pays ISO, ville, et pour la même source le titre et une description plus riche) ; (2) **Rituals perdait 577 offres sur 1 088** (latitude en chaîne dans une colonne Float) → coercion à la frontière comme pour les salaires ; (3) **WTTJ flux sitemap est mort** derrière le WAF Amazon (14 offres lues, 2 538 pages challengées — avant la brique WAF, ces pages passaient pour « pas une offre », en silence) ; l'API WTTJ par société fonctionne. 12 sources DEGRADED **préexistantes** (Hermès 609, L'Oréal 1 716, Kering 1 427 sans description…) : investigation en cours.

### D40 — Crons réactivés : ingest 00:00 Paris, refresh 04:00, reconcile lundi 05:00 (2026-09-07)
**Décision Loïc** : reprise des crons, mais « pas le matin, plutôt tard le soir, à partir de 00 h ». Posés et relus par deux chemins (mutation + requête projet) :
`catwalks-aggregator` **`0 22 * * *`** (00:00 Paris, été) · `catwalks-refresh` **`0 2 * * *`** (04:00) · `catwalks-reconcile` **`0 3 * * 1`** (lundi 05:00). `PIPELINE_CMD` vérifié par service, **aucune `INGEST_ONLY_KEYS` résiduelle**, heartbeat + digest Brevo armés sur l'aggregator.
**La marge de 4 h entre ingest et refresh est mesurée, pas supposée.** Durées réelles lues dans `SourceRun` (étalement des `ranAt` par grappe, 21 jours) : **150–210 min par run les 2–6 septembre** (avant l'orchestrateur parallèle), puis 138 min (994 sources, deux passes) et **34 min** (487 sources, hôtes déjà chauds juste après un run). Estimation retenue pour un run isolé après 24 h de silence : **40 min à 2 h**, pic possible au-delà (budget par source = 40 min). Une heure de marge aurait laissé le refresh démarrer pendant l'ingest et **fermer des offres vivantes non encore ré-attestées** (cf. les 1 855 offres du D37) : 4 h = le double du pire run post-correctifs.
**Effet de bord accepté** : à 22:00 UTC le run appartient au jour UTC précédent, le refresh de 02:00 au suivant → la photographie porte la date de **fin** du run, pas de son début. Cohérent (la photo décrit l'état après le run) ; à relire au passage à l'heure d'hiver (décalage +2 → +1, l'ingest tombera à 23:00 Paris).
**Reste ouvert (décision Loïc)** : la **licence de publication** des données de l'observatoire — aucune n'est affichée (une CC BY 4.0 écrite pendant le lot W1 a été retirée à l'audit, faute de décision). Trois postures : aucune mention (statu quo, pas de crédit ni de diffusion), **ouverte avec attribution** (reprise autorisée contre citation + lien — recommandation : chaque reprise devient un backlink, l'observatoire sert l'acquisition), fermée (protège une offre payante future, tue la citation).

### D41 — Licence CC BY 4.0 sur les données de l'observatoire (2026-09-07)
**Décision Loïc** : les chiffres publiés par Catwalks Intelligence sont réutilisables librement, y compris commercialement et modifiés, **à condition de citer Mode Careers avec un lien**. Motif retenu : l'observatoire sert l'acquisition, pas la vente directe — être cité partout avec un lien vaut plus que protéger des chiffres qui se recopient de toute façon ; chaque reprise devient un backlink. (Clôt la décision laissée ouverte par D38, où une CC BY écrite pendant le lot W1 avait été retirée à l'audit faute d'arbitrage.)
Implémentation : `DATA_LICENSE_URL` / `DATA_LICENSE_NAME` définis une seule fois (`lib/intelligence/seo.ts`), lus par le JSON-LD `Dataset` **et** par la mention visible — les deux ne peuvent pas diverger. Mention dans le bandeau `Coverage` (présent au pied de **chaque** page Intelligence : un lecteur qui reprend un chiffre depuis une page de pays ne lit pas la méthodologie) + section « Réutilisation des données » sur `/intelligence/methodologie` avec un exemple de citation. **Limite gravée** : la licence couvre les données publiées ici, **pas le texte des offres**, qui appartient aux employeurs.

### D42 — Logos : la taille et la transparence, pas le premier fournisseur qui répond (2026-09-07)
Trois défauts distincts signalés par Loïc (L'Oréal Professionnel, PVH, Lacoste, Estée Lauder, Penningtons, Buck Mason), tous mesurés sur les **120 Maisons les plus actives** avant correction :
- **Le second fournisseur n'était jamais atteint** tant que le premier répondait 200, même avec une vignette de 16 px. **57 Maisons sur 120** affichaient une image floue alors qu'une nette existait chez l'autre : Louis Vuitton 16→64 px, Hermès, Cartier, Dior, Nike, Ralph Lauren, Lacoste 32→64. Corrigé : les deux fournisseurs sont interrogés **en parallèle** (42 et 46 ms, coût nul) et le meilleur gagne.
- **Le garde ne regardait que le POIDS**, jamais les dimensions : un favicon 16×16 est une image valide de poids normal, illisible une fois agrandie dans une pastille de 48 px. Corrigé : `lib/image-size.ts` lit les dimensions dans l'en-tête ICO/PNG/JPEG/GIF (un SVG est vectoriel, donc toujours assez grand), seuil à **32 px** sous lequel la route rend 404 → le composant met son monogramme. 11 Maisons sur 120 sont dans ce cas (L'Oréal, PVH, URBN, Rituals, Estée Lauder Companies, Prada Group, Tom Ford) : **leurs sites ne publient rien de plus grand**, aucun fournisseur ne peut inventer les pixels.
- **Question de Loïc « Google n'est-il pas plus fiable ? »** — mesuré, et la réponse est non, il faut les deux : Google couvre plus de domaines (101 vs 93 utilisables) et gagne 53 fois en netteté, **mais il plafonne à 64 px** là où DuckDuckGo sert l'icône réelle du site — Ulta Beauty **1024 px**, Crocs / GANT / ONIVERSE / Pepco 256, Canada Goose 144 (que Google ignore). Passer par Google seul dégraderait 14 Maisons et en perdrait 2.
- **Régression attrapée par la capture, pas par le code** : prendre la plus grande image donnait à Ralph Lauren un **JPEG 64 px opaque** (cadre blanc visible dans la pastille) au lieu de son **ICO 48 px transparent**. Règle gravée (`lib/logo-choice.ts`) : à qualité comparable la transparence gagne, un format opaque ne l'emporte que s'il est **1,5× plus grand**. 18 tests unitaires.
**Reste ouvert (hors périmètre logo, remonté à Loïc)** : **652 Maisons sur 1 099** (28 % des offres actives) n'ont **aucun domaine** en base — H&M Group, Tapestry, Levi's, Parfums Chanel, Gucci, Saint Laurent, Coach, Balenciaga… : aucun logo n'est même tenté pour elles. Et « Estée Lauder » existe en **4 sociétés distinctes** (dont 3 sans domaine), « Lovi » est un nom tronqué de **Lovisa**, « VIA » (155 offres) est **VIA Rail Canada**, hors secteur.

### D43 — Le filet pointillé écrasait tout padding : `--rule-gap` (2026-09-07)
Loïc signalait pour la **deuxième fois** que les pastilles de filtre collaient au trait pointillé sur `/emplois`, malgré un réglage `pb-3` → `pb-5` la veille. **Mesuré au navigateur : l'écart réel valait 1 px, pas 20.** Cause : `.rule-b` dessine son filet en image de fond et impose lui-même `padding-bottom: 1px` pour donner au fond sa hauteur de tracé — cette règle **écrase tout `pb-*`** posé sur la même boîte. Le réglage de la veille n'a jamais rien changé.
Corrigé à la source : `.rule` / `.rule-b` prennent `var(--rule-gap, 1px)`, le 1 px reste le plancher technique, un écart plus grand se pose par la variable. `/emplois` : `--rule-gap: 24px`, **mesuré 24 px** après correction. Non-régression vérifiée : **134 filets sur 5 pages, 0 sans tracé**.
**Leçon de méthode** : un réglage de style se prouve par une **mesure du rendu** (`getComputedStyle` + capture), jamais par la lecture de la classe posée — deux sessions de suite ont cru corriger en changeant une valeur inopérante.

### D44 — Audit UX/UI desktop + mobile : trois défauts corrigés, la DA de référence rétablie (2026-09-07)
Audit demandé par Loïc, mené avec le skill `impeccable` : deux évaluations isolées en parallèle (revue de design / audit technique mesuré), sur le parcours candidat ET Catwalks Intelligence, en 1440 px et iPhone 13. Rapports : `data/audit-ux-a-design.md`, `data/audit-ux-b-technique.md`. **Note technique 16/20** (a11y 3, perf 3, responsive 3, tokens 4, intégrité 3) ; **Nielsen 3,1/4** (parcours candidat) et **3,3/4** (observatoire) ; spécificité de design 8/10.

**D17 est PÉRIMÉ sur le style, et c'est l'audit qui l'a prouvé.** J'avais transmis à l'agent les invariants de D17 (monochrome strict, graisse 400, titres en capitales, pastilles 100vmax) ; il est allé lire `design_2.md` (3 septembre, donc POSTÉRIEUR à D17) et a montré que la DA prescrit l'inverse : un accent coloré, un contraste serif/sans, des titres d'offres qui « ne sont **plus** en capitales » (§1 l.63), un rayon unique de 5 px et « **pas de pills** » (§1 l.62). `design_2.md` §0.1 liste précisément le monochrome et la graisse unique comme les **faiblesses de l'ancien site**. Un audit mené sur D17 aurait condamné tout ce qui a été bien fait. `apps/web/DESIGN.md` portait en plus une DA **Material 3 bleu Google** (1er septembre, refonte abandonnée le 3) : remplacé par un pointeur vers `design_2.md` + les deux pièges documentés.
**Écart ouvert, décision Loïc** : `design_2.md` fixe l'accent à `#105A33` (vert, 8.3:1) ; `globals.css:56` pose `--fa-green: #022026` (bleu-pétrole). Le nom dit « green », la valeur n'en est pas un. Aligner le code sur la DA, ou regraver la DA — non tranché.

**CRIT-1 — l'observatoire publiait un faux, sur SIX surfaces.** La colonne « Nouvelles · 30 j » recopiait exactement « Demande » : 24 995 offres de conseil de vente, dont « 24 995 nouvelles ce mois-ci », sous l'étiquette **FAIT OBSERVÉ**. Vérifié en prod sur les 11 lignes du tableau. Cause : le catalogue reconstruit le 06/09 ne contient **aucune offre de plus de 30 jours**, donc le filtre `firstSeenAt >= now() - 30 days` retient tout. Les KPI passaient déjà par `windowAvailable` ; les **tableaux, listes et la carte du monde, non** — métiers, groupes, secteurs, géographies, `WorldMapFrame` (un sélecteur entier « Nouvelles offres · 30 j » qui coloriait le monde sur ce faux) et `ProfileBlocks`. Corrigé à la source par `fmtNew()` (`format.ts`), qui rend `null` tant que l'observation ne couvre pas la fenêtre → « disponible à partir du 7 oct. 2026 ». Le sélecteur de la carte est masqué jusque-là. **Vérifié en exécution.**

**PERF — 4,4 Mo de vidéo servis puis jetés.** `/` servait **4 423 ko de MP4 à l'iPhone 13, soit 97,8 % du poids de la page**, y compris sous `prefers-reduced-motion` où la vidéo était ensuite masquée par `display:none` (`readyState=4`, `buffered=37 s`) : **masquer n'empêche pas de télécharger**. La décision remonte au rendu (`hooks/use-hero-video.ts`) : pas de vidéo sous `prefers-reduced-motion`, ni sous 768 px où le poster suffit. Mesuré après correction : **home mobile 4 523 ko → 100 ko (−97,8 %)**, reduced-motion 100 ko, desktop inchangé. La règle CSS reste en filet de sécurité pour l'instant avant hydratation.

**RESPONSIVE — deux écarts §6 sur le header.** Il restait à **114 px jusqu'à 320 px** (13,5 % de la hauteur d'un iPhone 13) et le burger basculait à **1023 px** au lieu de 768, parce que `site-nav.tsx` utilisait `lg:` (= 1024 en Tailwind) : toute la tablette en portrait avait une nav repliée que la DA veut déployée. Corrigé : `--header-h: 64px` sous 768 (la variable pilote `.page`, la barre sticky et le panneau détail), et les 7 points de bascule du header passés en `md:`. **Mesuré : 64 px à 390 et 767, 114 px et nav déployée à 768.**

**Trois faux positifs écartés, dont deux invalidaient l'outillage** — à retenir comme méthode : (a) « 258 cibles tactiles < 44 px » sur `/geographies` → **13 réelles** : 93 sont des pays d'une carte SVG nommés et focalisables avec **tableau alternatif sur la même page** (exemption WCAG 2.5.8), le reste des liens en ligne ; le vrai défaut est ailleurs — les **pastilles de filtre à 34 px**, contrôles principaux du produit, dont §7 prévoit le remède (`::before` tactile) **absent du CSS**. (b) « 405 éléments animent une propriété de layout » → **404 ont une durée de 0 s** : le détecteur lisait `transition-property` sans lire `transition-duration`. (c) « texte blanc à 1:1 sur le hero » → **5,85:1 dans le pire cas, conforme** : deux méthodes de mesure sur trois auraient gravé un faux critique ; seule la lecture du fond rendu, texte masqué, dit la vérité.

**Ce que l'audit valide, mesuré** : LCP 192–660 ms, **CLS 0,0000**, 0 `will-change` au repos, **0 débordement horizontal sur 18/18** combinaisons page × largeur, 0 élément interactif sans nom accessible, un seul H1 par page partout, focus conforme §7 sur 20 des 22 premiers arrêts, l'accent jamais seul porteur d'information, **0 couleur hors tokens** sur `/emplois` et `/entreprises`.

**Reste ouvert (décisions Loïc)** : l'aperçu des cartes d'offres (trois offres Ralph Lauren affichent le même texte d'entreprise, ce qui annule l'intérêt du split-view) ; le bouton « Filtres (n) » mobile de §4.3 et les ~970 px de vide avant la première offre ; la colonne latérale manquante de `/offre/[id]` (§5.3) ; le hero flou (§8 exige un sujet sectoriel identifiable) ; dépublier `/intelligence/marche` jusqu'au 7 octobre (6 KPI vides sur 8) ; l'ordre des CTA mobile, qui place « Matcher avec Catwalks » **au-dessus** de « Voir l'offre » et détourne l'intention du candidat par la mise en page — touche D14/D18, non tranché.

### D45 — Référentiel de domaines de Loïc : 328 domaines posés, 209 entités juridiques fusionnées (2026-09-07)
Constat remonté à Loïc le 2026-09-07 : **652 Maisons sur 1 099 (59 %) sans domaine**, soit 28 % des offres actives sans logo possible — et surtout des **entités juridiques prises pour des Maisons** (Mango en 32 sociétés, Nike 37, Swarovski 31). La résolution automatique (`resolve-domains`, catalogue + Wikidata) n'en réglait que **48 sur 652 (7 %)** : Loïc a établi le référentiel à la main (`data/domaines_officiels_maisons.xlsx` → `data/maisons-domaines-loic.tsv`, 652 lignes, 3 statuts).

**Trois statuts, trois traitements — la distinction est l'essentiel** (`applyDomainSheet.ts`, commande `apply-domain-sheet`, `--apply` pour écrire) :
- **IDENTIFIÉ** (320) → on pose le domaine, le logo suit.
- **RATTACHÉ** (225) → la ligne n'est PAS une Maison : « MANGO TR TEKSTIL TICARET LTD.S », « NIKE Korea LLC », « Michael Kors (Portugal) ». Poser le domaine aurait affiché le bon logo **en laissant 32 « Mango » dans les filtres, les classements et l'observatoire**. On FUSIONNE l'identité dans la marque mère (id, URL et `firstSeenAt` des offres conservés, D22 : seul le préfixe de la clé de cluster change).
- **À VÉRIFIER** (107) → nom trop ambigu (« B2 », « KENT », « Public ») : on ne touche à rien, monogramme. _Un domaine deviné est un logo faux ([[slug-probe-false-positives]])._

**Exécuté en prod** : 328 domaines posés, **209 fusions, 2 673 offres déplacées**, 0 Maison introuvable, 0 conflit. Résultat vérifié : **Maisons 1 099 → 890**, sans domaine **652 → 115**, **offres actives 70 131 inchangées** (rien créé, rien perdu). Mango 551 → **1 579**, Nike 716 → **1 046**, Michael Kors 270 → **525**, Swarovski 174 → **502**, Coach 223 → **379**, Condé Nast 9 → **148**. _Ce ne sont pas des chiffres gonflés : ce sont les vrais — l'ancien « Mango : 551 » était le faux._

**Effet sur l'observatoire, traité** (question de Loïc) : les photographies déjà prises portent l'ANCIEN découpage ; comparées aux suivantes elles auraient affiché « Mango +1 028 en un jour », une variation qui n'a jamais eu lieu. Les **2 764 lignes `MarketSnapshot` de périmètre `company` sont supprimées** (`ops-drop-company-snapshot.mts`) : l'histoire par société repart demain sur une base juste. **8 262 lignes conservées** — une offre Mango est restée dans la même ville, le même pays, le même métier, donc ville/pays/métier/secteur gardent tout leur historique.
**Pièges rencontrés, gravés** : (a) Excel **omet la cellule d'une colonne vide** → le domaine glissait en colonne 4 pour les Maisons sans groupe, lire la colonne 5 aveuglément aurait posé 500 domaines vides ; l'extraction doit suivre les **références de cellule** (`r="E3"`), et gérer les cellules **auto-fermantes** (`<c r="D3" />`). (b) La mère est nommée « NIKE »/« UNIQLO » en base, « Nike »/« Uniqlo » dans la note → index **insensible à la casse**, à nom identique la société la plus fournie gagne. (c) « rattaché à lvmh.com » nomme un **domaine**, pas une société : refusé, sinon on fusionnait vers une société inexistante.

### D46 — Le padding latéral mobile : Tailwind écrasait `.container`, et 5 règles écrasaient le reste (2026-09-07)
Signalé par Loïc (« le padding left/right sur mobile c'est une cata ») — **l'audit UX/UI ne l'avait pas vu** : il mesurait le débordement horizontal (0 partout, exact) et les cibles tactiles, **jamais les marges latérales**. Angle mort réel du protocole.
Mesuré sur iPhone 13 : le texte touchait le bord de l'écran à **0 px** sur `/`, `/entreprises` et les pages Intelligence ; seule `/emplois` avait ses 24 px. **Deux causes empilées** :
1. **`.container` est AUSSI une classe Tailwind v4**, générée après notre feuille (`@import 'tailwindcss'` en tête) : son `width:100%` + sa cascade de `max-width` écrasaient et la largeur de DA (1160 px) et les 24 px de marge. Prouvé dans le CSS servi en prod (`.container{width:100%}` puis `.container{max-width:40rem}`…). `/emplois` échappait au bug en utilisant `.container-wide`, nom non revendiqué par Tailwind. Corrigé par `:where(html) .container` — spécificité relevée sans peser sur la cascade interne.
2. **Cinq règles écrivaient `padding` sur quatre côtés** là où elles ne voulaient régler que le vertical : `.section`, `.page-head`, `.intel-hero`, `.maison`, `.searchbar` remettaient les 24 px latéraux à zéro quand elles étaient posées sur un `.container`. Passées en `padding-top`/`padding-bottom`. **Même piège que `.rule-b` (D43) le matin même : une règle qui écrase plus large que son intention.**
**Vérifié après correction : 24 px à gauche sur les 6 pages, en iPhone 13 ET en 320 px, 0 débordement de page.** Les valeurs négatives à droite sur `/metiers` et `/geographies` sont les **tableaux larges qui défilent dans `.itable-wrap`** (964 px dans 342 px, page non débordée) — comportement voulu, faux positif écarté. La nav Intelligence qui « sortait de l'écran » est de même un **carrousel horizontal assumé** (`overflow-x:auto`, liens de 44 px) : second faux positif écarté.
**Leçon de méthode** : un audit qui ne mesure pas une dimension ne dit rien sur elle. « 0 débordement horizontal » ne prouve pas que les marges existent.
