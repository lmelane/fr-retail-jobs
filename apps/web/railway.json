{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": ["node /migration/node_modules/prisma/build/index.js migrate deploy --schema /migration/prisma/schema.prisma"],
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120
  }
}
